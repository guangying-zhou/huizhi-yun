HZY_WEBDEV_DEV_AGENT_URL=http://127.0.0.1:19090
HZY_WEBDEV_DEV_AGENT_TOKEN=<dev-agent-token>

HZY_CONSOLE_URL=http://localhost:3000
HZY_WEBDEV_REQUIRE_APP_GRANT=false

# WebDev metadata DB access goes through hzy-data-runtime /v1/webdev/*.
# Do not configure DB_HOST/DB_USER/DB_PASSWORD in WebDev.
# Static bearer is only for explicit local/PoC mode. Managed tenant-runtime
# uses the Console-issued webdev.runtime identity instead.
HZY_WEBDEV_DATA_RUNTIME_URL=http://127.0.0.1:18080
HZY_WEBDEV_DATA_RUNTIME_TOKEN=<local-data-runtime-token>

NUXT_PUBLIC_APP_DISPLAY_NAME=WebDev
