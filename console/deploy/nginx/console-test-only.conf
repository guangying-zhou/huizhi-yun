# Nginx HTTP-only config for shared console-test when console-prod runs on Cloudflare.
# Domain:
#   hzy-test.wiztek.cn -> console-test on 127.0.0.1:3031
#
# Use this template instead of console-wiztek.conf when hzy.wiztek.cn is routed
# to Cloudflare Worker. Do not install both templates to Nginx at the same time.
#
# Install example:
#   sudo mkdir -p /var/www/certbot
#   sudo cp deploy/nginx/console-test-only.conf /etc/nginx/conf.d/console-wiztek.conf
#   sudo nginx -t
#   sudo systemctl reload nginx
#
# Certificate bootstrap:
#   sudo certbot certonly --webroot -w /var/www/certbot -d hzy-test.wiztek.cn

map $http_upgrade $hzy_console_test_connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    listen [::]:80;
    server_name hzy-test.wiztek.cn;

    client_max_body_size 100m;

    access_log /var/log/nginx/console-test.access.log;
    error_log /var/log/nginx/console-test.error.log warn;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        proxy_pass http://127.0.0.1:3031;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Forwarded-Port 80;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $hzy_console_test_connection_upgrade;

        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_buffering off;
    }
}
