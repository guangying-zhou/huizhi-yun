import { enterpriseAimsPersonnel } from './enterpriseAimsPersonnel'
import{createError,getHeader,getQuery,readBody,setHeader,type H3Event}from'h3';import{callEnterpriseRuntime,enterpriseRuntimePermitExpiresAt,prepareEnterpriseRuntime,requireEnterpriseUser}from'@hzy/foundation/server/utils/enterpriseRuntimeClient';import{loadScopedAuthorizationFromConsoleRuntime}from'@hzy/foundation/server/utils/platformBundleAuthorization'
const keys=new Set(['projectCode','name','shortName','internalCode','description','category','methodology','portfolioId','domainCode','deptCode','leaderUid','securityLevel','confidentialityLevel','accessWhitelist','startDate','endDate','serviceLineCode','servicePeriodSeq','servicePeriodStart','servicePeriodEnd','servicePeriodLabel','templateVersionId','excludedTemplateWorkItemKeys'])
export async function enterpriseAimsProjectCreate(event:H3Event){setHeader(event,'Cache-Control','no-store');if(Object.keys(getQuery(event)).length)throw createError({statusCode:400,message:'项目创建参数无效'});const key=String(getHeader(event,'Idempotency-Key')||'').trim();if(!key||key.length>191)throw createError({statusCode:400,message:'缺少有效操作标识'});const input=await readBody(event);if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).some(k=>!keys.has(k)))throw createError({statusCode:400,message:'项目创建参数无效'});const user=await requireEnterpriseUser(event),scoped=await loadScopedAuthorizationFromConsoleRuntime(event,user.uid,'aims',{resourceCode:'projects',action:'create'});if(scoped.decision?.allowed!==true)throw createError({statusCode:403,message:'当前用户没有项目创建权限'});await prepareEnterpriseRuntime(event,'aims.project-create');const personnel=await enterpriseAimsPersonnel(event,user,input,'leaderUid','projects','new','create');return await callEnterpriseRuntime(event,'aims.project-create',{personnel,tenant:user.tenant,deployment:user.deployment,input,authorization:{actorUid:user.uid,tenant:user.tenant,deployment:user.deployment,resource:'projects',action:'create',allowed:true,expiresAt:enterpriseRuntimePermitExpiresAt()}},{idempotencyKey:key})}
