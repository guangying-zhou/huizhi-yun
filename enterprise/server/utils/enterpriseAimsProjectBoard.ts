import{createError,getQuery,getRouterParam,setHeader,type H3Event}from'h3'
import{callEnterpriseRuntime,enterpriseRuntimePermitExpiresAt,prepareEnterpriseRuntime,requireEnterpriseUser}from'@hzy/foundation/server/utils/enterpriseRuntimeClient'
import{loadAuthorizationSnapshotFromConsoleRuntime}from'@hzy/foundation/server/utils/platformBundleAuthorization'
import{authorizationResourcesAllow}from'@hzy/foundation/shared/utils/authorizationActions'
import{enterpriseAimsProjectScope}from'./enterpriseAimsProjects'
const numericID=/^[1-9]\d*$/
export async function enterpriseAimsProjectBoard(event:H3Event){setHeader(event,'Cache-Control','no-store');if(Object.keys(getQuery(event)).length)throw createError({statusCode:400,message:'项目看板不接受筛选参数'});const projectId=String(getRouterParam(event,'id')||'').trim();if(!numericID.test(projectId)||!Number.isSafeInteger(Number(projectId)))throw createError({statusCode:400,message:'项目标识无效'});const user=await requireEnterpriseUser(event);const authorization=await loadAuthorizationSnapshotFromConsoleRuntime(user.uid,'aims',event);if(!authorizationResourcesAllow(authorization.resources,'work_items','view',authorization.actionPolicies?.work_items))throw createError({statusCode:403,message:'无项目看板查看权限'});await prepareEnterpriseRuntime(event,'aims.project-board-view');const scope=await enterpriseAimsProjectScope(event,user.uid);return await callEnterpriseRuntime(event,'aims.project-board-view',{tenant:user.tenant,deployment:user.deployment,projectId,query:scope,authorization:{actorUid:user.uid,tenant:user.tenant,deployment:user.deployment,resource:'project-board',action:'view',expiresAt:enterpriseRuntimePermitExpiresAt()}})}
