import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8')
test('project create bridge requires exact permission, scope decision and idempotency key',()=>{const bff=read('server/utils/enterpriseAimsProjectCreate.ts');assert.match(bff,/Idempotency-Key/);assert.match(bff,/resourceCode:'projects',action:'create'/);assert.match(bff,/decision\?\.allowed!==true/);assert.match(bff,/aims\.project-create/);assert.doesNotMatch(bff,/members|milestones|board/)})
test('project create domain keeps receipt, audit and creation in one transaction',()=>{const command=read('../data-runtime/internal/apps/aims/enterprise_project_create.go'),domain=read('../data-runtime/internal/apps/aims/product_versions.go');assert.match(command,/ExecuteInTransaction\(ctx, tx/);assert.match(command,/createProjectWithProductBindingTx\(ctx, tx/);assert.match(command,/tx\.Commit\(\)/);assert.match(domain,/INSERT INTO project_lifecycle_events/);assert.match(domain,/source\s*\) VALUES \(\?, NULL, \?, UTC_TIMESTAMP\(6\), \?, 'aims\.project\.create'\)/s)})
test('new project layer does not expose member, plan or board writes',()=>{const page=read('../aims/layer/pages/enterprise-project-new.vue');assert.match(page,/Idempotency-Key/);assert.doesNotMatch(page,/(?:members|milestones|board|work-items)/)})
